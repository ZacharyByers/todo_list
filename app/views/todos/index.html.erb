<h1>To Do</h1>
<% if @todos.any? %>
  <% @todos.each do |todo| %>
    <h3>
      <%= link_to todo.title, todo_path(todo) %>
    </h3>
  <% end %>

  <% else %>
  <h4> Nothing to do!</h4>
<% end %>

<%= link_to 'New Task', new_todo_path, class: 'btn' %>
